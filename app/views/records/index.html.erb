<% content_for :content_header do %>
    <section class="content-header">
      <p id="notice"><%= notice %></p>
      <div class="row">
        <h1>Historial</h1>
      </div>
    </section>
<% end %>

<section class="content invoice">
<div class="table-responsive">
<table border="1" class="table table-striped">
  <thead>
    <tr>
      <th>Usuario</th>
      <th>Beneficiario</th>
      <th>Estado</th>
      <th>Objetivo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |record| %>
      <tr>
        <td><%= record.users %></td>
        <td><%= record.beneficiary.nombre %></td>
        <td><%= record.state.nombre %></td>
        <td><%= record.objective.texto %></td>
        <td><%= link_to 'Mostrar', record %></td>
        <td><%= link_to 'Editar', edit_record_path(record) %></td>
        <td><%= link_to 'Eliminar', record, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</section>
<br>

<%= link_to 'Nuevo Historial', new_record_path %>
